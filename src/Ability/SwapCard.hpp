#ifndef SWAPCARD_HPP
#define SWAPCARD_HPP
#include "Ability.hpp"

// SWAPCARD class
class SwapCard : public Ability
{
public:
    void use(int _idAbility, PlayerQueue *p, Gamestate *g)
    {
    }
};

#endif